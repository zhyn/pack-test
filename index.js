// import * as utils from './lib/utils';

// export default {
//     utils : utils,
// }

const globalToos = {
    sendText : function(name){
        console.log('hello' + name);
    }
}
module.exports = globalToos;